---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install basic tools
  ansible.builtin.apt:
    name: "{{ pve_basics_packages }}"
    state: present
